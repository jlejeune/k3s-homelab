---
vars:
  - clusterConfigMap:
      name: cluster-settings
      namespace: default
      key: NAS_ADDRESS
      targetPath: NAS_ADDRESS

deployments:
  - path: snapshot-controller
  - barrier: true
  - path: nfs-provisioner
  - path: longhorn
