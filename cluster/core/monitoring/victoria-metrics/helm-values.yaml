---
server:
  retentionPeriod: 7d
  persistentVolume:
    enabled: true
    storageClassName: longhorn
    size: 10Gi
  resources:
    requests:
      cpu: 600m
      memory: 640Mi
    limits:
      cpu: 1000m
      memory: 1Gi
  extraArgs:
    envflag.enable: "true"
    envflag.prefix: VM_
    loggerFormat: json
