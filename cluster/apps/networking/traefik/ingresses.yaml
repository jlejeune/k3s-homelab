---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: nas
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: {{SECRET_DYNAMIC_DOMAIN}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`nas.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: security-header
          namespace: networking
      services:
        - kind: Service
          name: nas
          namespace: networking
          port: 443
          scheme: https
          serversTransport: insecure-skip-verify
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: webdav
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: {{SECRET_DYNAMIC_DOMAIN}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`webdav.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: security-header
          namespace: networking
      services:
        - kind: Service
          name: webdav
          namespace: networking
          port: 443
          scheme: https
          serversTransport: insecure-skip-verify
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: mealie
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: {{SECRET_DYNAMIC_DOMAIN}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`mealie.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: security-header
          namespace: networking
      services:
        - kind: Service
          name: mealie
          namespace: mealie
          port: 9000
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: jellyfin
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: {{SECRET_DYNAMIC_DOMAIN}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`videos.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: security-header
          namespace: networking
      services:
        - kind: Service
          name: jellyfin
          namespace: jellyfin
          port: 8096
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: plik
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: {{SECRET_DYNAMIC_DOMAIN}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`plik.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: security-header
          namespace: networking
      services:
        - kind: Service
          name: plik
          namespace: plik
          port: 8080
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: zigbee2mqtt
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: {{SECRET_DYNAMIC_DOMAIN}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`zigbee2mqtt.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: forwardauth-authelia
          namespace: networking
        - name: security-header
          namespace: networking
      services:
        - kind: Service
          name: zigbee2mqtt
          namespace: home-automation
          port: 8080
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: home-assistant
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: {{SECRET_DYNAMIC_DOMAIN}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`ha.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: forwardauth-authelia
          namespace: networking
        - name: security-header
          namespace: networking
      services:
        - kind: Service
          name: home-assistant
          namespace: home-automation
          port: 80
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: dashy
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: {{SECRET_DYNAMIC_DOMAIN}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`home.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: forwardauth-authelia
          namespace: networking
        - name: security-header
          namespace: networking
      services:
        - kind: Service
          name: dashy
          namespace: dashy
          port: 8080
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: joplin
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: {{SECRET_DYNAMIC_DOMAIN}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`joplin.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: security-header
          namespace: networking
      services:
        - kind: Service
          name: joplin
          namespace: joplin
          port: 22300
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: freshrss
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: {{SECRET_DYNAMIC_DOMAIN}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`rss.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: security-header
          namespace: networking
      services:
        - kind: Service
          name: freshrss
          namespace: freshrss
          port: 80
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: immich
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: {{SECRET_DYNAMIC_DOMAIN}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`photos.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: security-header
          namespace: networking
      services:
        - kind: Service
          name: immich-server
          namespace: immich
          port: 80
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: authelia
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: {{SECRET_DYNAMIC_DOMAIN}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`auth.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: security-header
          namespace: networking
      services:
        - kind: Service
          name: authelia
          namespace: authelia
          port: 80
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRouteUDP
metadata:
  name: wireguard
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/hostname: vpn.{{SECRET_DOMAIN}}
    external-dns.alpha.kubernetes.io/target: {{SECRET_DYNAMIC_DOMAIN}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - wireguard
  routes:
    - services:
        - name: wireguard
          namespace: networking
          port: 51820
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-dashboard
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/hostname: traefik.{{SECRET_DOMAIN}}
    external-dns.alpha.kubernetes.io/target: {{LB_TRAEFIK_ADDRESS}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`traefik.{{SECRET_DOMAIN}}`) && (PathPrefix(`/dashboard`) || PathPrefix(`/api`))"
      services:
        - name: api@internal
          kind: TraefikService
      middlewares:
        - name: forwardauth-authelia
          namespace: networking
        - name: security-header
          namespace: networking
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: longhorn
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: {{LB_TRAEFIK_ADDRESS}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`longhorn.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: forwardauth-authelia
          namespace: networking
        - name: security-header
          namespace: networking
      services:
        - name: longhorn-frontend
          namespace: storage
          port: 80
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: code-server
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/hostname: ha-code.{{SECRET_DOMAIN}}
    external-dns.alpha.kubernetes.io/target: {{LB_TRAEFIK_ADDRESS}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`ha-code.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: forwardauth-authelia
          namespace: networking
      services:
        - kind: Service
          name: code-server
          namespace: home-automation
          port: 8443
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: grafana
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: {{LB_TRAEFIK_ADDRESS}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`grafana.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: forwardauth-authelia
          namespace: networking
        - name: security-header
          namespace: networking
      services:
        - name: grafana
          namespace: monitoring
          port: 80
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: victoria-metrics
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: {{LB_TRAEFIK_ADDRESS}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`victoria-metrics.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: forwardauth-authelia
          namespace: networking
        - name: security-header
          namespace: networking
      services:
        - name: victoria-metrics-single-server
          namespace: monitoring
          port: 8428
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: open-webui
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: {{LB_TRAEFIK_ADDRESS}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`chat.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: security-header
          namespace: networking
      services:
        - kind: Service
          name: open-webui
          namespace: home-automation
          port: 8080
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: maintenant
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: {{LB_TRAEFIK_ADDRESS}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`maintenant.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: forwardauth-authelia
          namespace: networking
        - name: security-header
          namespace: networking
      services:
        - kind: Service
          name: maintenant
          namespace: monitoring
          port: 8080
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: uptime-kuma
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: {{LB_TRAEFIK_ADDRESS}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`status.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: forwardauth-authelia
          namespace: networking
        - name: security-header
          namespace: networking
      services:
        - kind: Service
          name: uptime-kuma
          namespace: monitoring
          port: 3001
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: esphome-pna-preflight
  namespace: networking
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`esphome.{{SECRET_DOMAIN}}`) && Method(`OPTIONS`)"
      priority: 100
      middlewares:
        - name: private-network-access
          namespace: networking
      services:
        - kind: Service
          name: preflight-responder
          namespace: networking
          port: 80
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: esphome
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/hostname: esphome.{{SECRET_DOMAIN}}
    external-dns.alpha.kubernetes.io/target: {{LB_TRAEFIK_ADDRESS}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`esphome.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: forwardauth-authelia
          namespace: networking
        - name: private-network-access
          namespace: networking
      services:
        - kind: Service
          name: esphome
          namespace: home-automation
          port: 6052
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: supabase
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: {{SECRET_DYNAMIC_DOMAIN}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`supabase.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: security-header
          namespace: networking
      services:
        - kind: Service
          name: supabase-supabase-kong
          namespace: supabase
          port: 8000
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: nextcloud
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: {{SECRET_DYNAMIC_DOMAIN}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`nextcloud.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: security-header
          namespace: networking
      services:
        - kind: Service
          name: nextcloud
          namespace: nextcloud
          port: 80
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: calibre-web-automated
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: {{LB_TRAEFIK_ADDRESS}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`calibre.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: forwardauth-authelia
          namespace: networking
        - name: security-header
          namespace: networking
      services:
        - kind: Service
          name: calibre-web-automated
          namespace: media-automation
          port: 8083
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: calibre-web-automated-opds
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: {{LB_TRAEFIK_ADDRESS}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`calibre.{{SECRET_DOMAIN}}`) && PathPrefix(`/opds`)"
      middlewares:
        - name: security-header
          namespace: networking
      services:
        - kind: Service
          name: calibre-web-automated
          namespace: media-automation
          port: 8083
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: collabora
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: {{SECRET_DYNAMIC_DOMAIN}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`collabora.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: security-header
          namespace: networking
      services:
        - kind: Service
          name: collabora
          namespace: nextcloud
          port: 9980
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: lam
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: {{SECRET_DYNAMIC_DOMAIN}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`lam.{{SECRET_DOMAIN}}`)
      services:
        - kind: Service
          name: lam
          namespace: openldap
          port: 80
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: sonarr
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: "{{LB_TRAEFIK_ADDRESS}}"
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`sonarr.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: forwardauth-authelia
          namespace: networking
        - name: security-header
          namespace: networking
      services:
        - kind: Service
          name: sonarr
          namespace: media-automation
          port: 8989
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: radarr
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: "{{LB_TRAEFIK_ADDRESS}}"
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`radarr.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: forwardauth-authelia
          namespace: networking
        - name: security-header
          namespace: networking
      services:
        - kind: Service
          name: radarr
          namespace: media-automation
          port: 7878
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: prowlarr
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: "{{LB_TRAEFIK_ADDRESS}}"
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`prowlarr.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: forwardauth-authelia
          namespace: networking
        - name: security-header
          namespace: networking
      services:
        - kind: Service
          name: prowlarr
          namespace: media-automation
          port: 9696
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: stirling-pdf
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: {{SECRET_DYNAMIC_DOMAIN}}
    kubernetes.io/ingress.class: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: "Host(`pdf.{{SECRET_DOMAIN}}`) && PathPrefix(`/`)"
      middlewares:
        - name: forwardauth-authelia
          namespace: networking
        - name: security-header
          namespace: networking
      services:
        - kind: Service
          name: stirling-pdf
          namespace: stirling-pdf
          port: 8080
          scheme: http
  tls:
    secretName: "{{SECRET_DOMAIN}}-tls"
